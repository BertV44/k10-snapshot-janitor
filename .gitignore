# Reports and run output: never commit these, they can carry real namespace,
# application and policy names.
reports/
k10-janitor-reports/
*.prom
k10-janitor-*.csv
k10-janitor-*.jsonl
k10-janitor-*.jsonl.audit
k10-janitor-*.summary.txt

# Kubeconfig and secrets
kubeconfig
*.kubeconfig
.kube/
*.pem
*.key
*.token

# Kasten support bundles
*-support-bundle*.tar.gz
k10_debug*

# Local artifacts
.DS_Store
*.swp
*~
/tmp/

# Project instructions for Claude Code: local only, never published
CLAUDE.md
